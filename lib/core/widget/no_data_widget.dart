import 'package:flutter/material.dart';

class NoDataListWidget extends StatelessWidget {
  const NoDataListWidget({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('No Data Tag'),
    );
  }
}
